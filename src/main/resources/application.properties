# Server Configuration
server.port=8080

# MySQL Database (Connecting to Clever Cloud via Environment Variables)
spring.datasource.url=${MYSQL_URL}
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Optimization
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# Hikari Connection Pool (Free Tier Memory ke liye)
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1

# CORS Configuration (Local aur Production dono ke liye)
# Jab aapka Frontend deploy ho jaye, toh uska URL bhi yahan comma laga kar add kar dena
spring.web.cors.allowed-origins=http://127.0.0.1:5500, http://localhost:5173, https://your-frontend-link.vercel.app

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=tushardhakad2004@gmail.com
spring.mail.password=yydbrndugfkqovkq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Google OAuth2 Configuration
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email

cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api_key=${CLOUDINARY_API_KEY}
cloudinary.api_secret=${CLOUDINARY_API_SECRET}

# App URL Configuration
app.backend.url=${APP_BACKEND_URL}